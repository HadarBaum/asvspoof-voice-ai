# Core
setuptools==75.1.0  # provides the distutils shim pyspark's toPandas() needs on Python 3.12 (distutils was removed from stdlib)
numpy==1.26.4
pandas==2.2.2
pyarrow==16.1.0
scipy==1.13.1

# Audio feature extraction
librosa==0.10.2.post1
soundfile==0.12.1

# Machine learning
scikit-learn==1.5.0
joblib==1.4.2

# Big data / infra clients
pyspark==3.5.9  # 3.5.1 crashes ("Python worker exited unexpectedly") on Windows + Python 3.12; fixed in later 3.5.x
boto3==1.34.131
kafka-python-ng==2.2.2  # kafka-python's vendored six is broken on Python 3.12+; this fork fixes it, same API
elasticsearch==8.14.0

# Web app
flask==3.0.3

# Insights / docs
matplotlib==3.9.0
python-pptx==0.6.23

# Dataset download
kaggle==2.2.4
